'use client'

import PostCustom from "@/components/PostCustom";

const Page = () => {

    return(
        <PostCustom title={''} content={''} listMedia={[]} listTag={[]} publish={false} where={'new'}/>
    )

};

export default Page;